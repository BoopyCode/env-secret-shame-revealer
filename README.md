# ENV Secret Shame Revealer

Finds .env files with exposed secrets before they become public embarrassment. Run: python env_shame_revealer.py

Scans for API keys, passwords, and other secrets that shouldn't be committed to git.